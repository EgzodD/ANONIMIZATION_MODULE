# Исключаем из build-контекста тяжёлое и лишнее.
# ВАЖНО: models/person_ruBERT НЕ исключаем — модель нужна внутри образа.
.git/
.venv/
venv/
**/__pycache__/
*.pyc
*.pyo

# Локальный .env с ХОСТОВЫМ путём — в контейнере путь другой (см. docker-compose).
.env

# GramLynx — отдельный сервис, в образ анонимизации не нужен (гигабайты моделей).
tests/GramLynx/

# Архивы моделей (сама распакованная модель в models/ остаётся).
*.zip

# Кэши/отчёты
.pytest_cache/
.ruff_cache/
.mypy_cache/
